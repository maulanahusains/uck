package com.example.one

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.TextView

class Halaman1 : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_halaman1)

//        val nnama = intent.getStringExtra("nama") ?: "Data tidak tersedia"
//        val nkelas = intent.getStringExtra("kelas") ?: "Data tidak tersedia"
//        val nalamat = intent.getStringExtra("alamat") ?: "Data tidak tersedia"
    }
}